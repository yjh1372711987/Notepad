

public class Notepad {
    int id;
    String Notepad_Content;
    String Notepad_Time;

    @Override
    public String toString() {
        return "Notepad [id=" + id + ", Notepad_Content=" + Notepad_Content + ", Notepad_Time=" + Notepad_Time + "]";
    }
}
